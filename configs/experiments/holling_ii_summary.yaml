# Experiment: Holling type-II + summary features (no embedding) + process noise.

extends: ../base.yaml

inference:
  feature_mode: summary
  include_holling: true
  include_process_noise: true
  prior:
    log_sigma_p: [-6.907755278982137, -2.120263536200091]
