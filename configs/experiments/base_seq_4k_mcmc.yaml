# Experiment: 2-round SNPE + MCMC posterior sampling.

extends: ../base.yaml

inference:
  num_simulations: 4000
  sample_with: mcmc
  mcmc_method: slice_np
  sequential:
    num_rounds: 2
    prior_mixin: 0.2
    simulations_per_round: [2000, 2000]
